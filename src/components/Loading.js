/** @jsx jsx */
import { jsx } from '@emotion/core'

    const marginStyle = {
        margin: 'auto',
        textAlign: 'center',
        fontSize: 20,
    }
export const Loading = () => <p css={marginStyle}>Loading...</p>
